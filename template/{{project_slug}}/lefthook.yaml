commit-msg:
  commands:
    commitlint:
      run: commitlint --edit $1
pre-commit:
  commands:
    lint-docker:
      glob: "Dockerfile"
      run: task lint-docker
    lint-shell:
      glob: "*.sh"
      run: task lint-shell
